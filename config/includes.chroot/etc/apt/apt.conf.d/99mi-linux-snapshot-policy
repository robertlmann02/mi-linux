// MI Linux uses Debian snapshots for the Founder channel so normal
// systems stay on the quarterly six-month delayed package target.
// Snapshot Release files are intentionally historical, so apt must not
// reject them only because their Valid-Until date has passed.
Acquire::Check-Valid-Until "false";
